const { requestAudiusNodes } = require('../station')

describe('tests for station stuff', () => {
  it('gets an audius node endpoint from the api', () => {
    // TODO: write mock functions to test this with
  })

  it.todo('gets the song data from the track')
  it.todo('reads the metadata from the track')
})